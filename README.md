# task-manager
A simple Task Manager application built with Django. It provides RESTful APIs to manage tasks, supporting basic CRUD operations and user authentication.  Features Create, update, delete, and view tasks Mark tasks as completed or pending User authentication for secure access Auto-tracked timestamps for task creation and updates.
